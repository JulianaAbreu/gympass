import reducer from './reducer';

export { default as repositoriesTypes } from './types';
export { default as repositoriesActions } from './actions';
export { default as repositoriesSelectors } from './selectors';

export default reducer;
